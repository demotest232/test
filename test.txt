 @SuppressWarnings("unchecked")
    private void setSqlQueries(Map<OperationTypeEnum, Map<String, List<List<Object>>>> newMap) {
        try {
            Field field = GridModifyQueryBuilderImpl.class.getDeclaredField("sqlQueries");
            field.setAccessible(true);

         
            Map<OperationTypeEnum, Map<String, List<List<Object>>>> existing =
                    (Map<OperationTypeEnum, Map<String, List<List<Object>>>>) field.get(builder);

         
            existing.clear();
            existing.putAll(newMap);
        } catch (Exception e) {
            throw new RuntimeException("Failed to modify sqlQueries", e);
        }
    }
