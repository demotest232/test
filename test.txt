The official r2dbc-mssql driver only supports username/password style logins; integrated / Entra ID (AAD) authentication modes such as ActiveDirectoryPassword, ActiveDirectoryManagedIdentity, or ActiveDirectoryDefault are not implemented.
