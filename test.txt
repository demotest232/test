String oldContent = new String(
        repo.getFileContent(path, fromTag).read().readAllBytes(),
        StandardCharsets.UTF_8
);

String newContent = new String(
        repo.getFileContent(path, toTag).read().readAllBytes(),
        StandardCharsets.UTF_8
);
