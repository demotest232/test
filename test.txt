 Logger mockLogger = mock(Logger.class);

        try (MockedStatic<LoggerFactory> loggerFactoryMock = mockStatic(LoggerFactory.class)) {
           
            loggerFactoryMock
                    .when(() -> LoggerFactory.getLogger(GridModifyQueryBuilderImpl.class))
                    .thenReturn(mockLogger);

          
            when(mockLogger.isDebugEnabled()).thenReturn(true);

         
            GridModifyQueryBuilderImpl builder = new GridModifyQueryBuilderImpl(null, null);
            builder.build(); // or whichever method logs debug output

        
            verify(mockLogger, atLeastOnce()).isDebugEnabled();
        }
