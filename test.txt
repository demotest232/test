// Save to disk (proper InputStream copy)
        try (InputStream in = content.read();
             OutputStream os = new FileOutputStream(outputFile, false)) {
            byte[] buffer = new byte[8192];
            int len;
            while ((len = in.read(buffer)) != -1) {
                os.write(buffer, len);
            }
        }
