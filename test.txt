int dmIndex = path.indexOf("/DM/") + 4;
    int nextSlash = path.indexOf("/", dmIndex);
    String requestNbr;
    if (nextSlash != -1) {
        requestNbr = path.substring(dmIndex, nextSlash);
    } else {
        requestNbr = path.substring(dmIndex); // till end if no more "/"
    }
