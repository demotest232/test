 @Test
    void testUpdateThrowsException() {
        AuditEventPropertyDAOImpl daoImpl = new AuditEventPropertyDAOImpl(null);

        AuditEventProperty dummy = new AuditEventProperty("name", "value");
        assertThrows(UnsupportedOperationException.class, () -> daoImpl.update(dummy));
    }
